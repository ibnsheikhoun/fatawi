let open = document.querySelector('#open'); let nav = document.querySelector('#nav'); open.addEventListener('click', () => { nav.classList.toggle('nav_up'); if (nav.classList.contains('nav_up')) { open.style.color = "var(--color_light)"; } else { open.style.color = ""; } }); document.addEventListener('click', (event) => { if (!nav.contains(event.target) && event.target !== open) { nav.classList.remove('nav_up'); open.style.color = ""; } });